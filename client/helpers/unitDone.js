Template.registerHelper('unitDone', function() {
    /*    if (Meteor.userId()) {
            var node = this.rid;
            var user_address = Meteor.users.findOne({
                '_id': Meteor.userId()
            }).emails[0].address;
            var items = people.findOne({
                'email': user_address
            }).mind;
            //console.log(items[node].state);
            if (items[node].succeeded > 0) return true;
            else return false;
        } else return false;*/
});
