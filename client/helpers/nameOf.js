Template.registerHelper('nameOf', function(rid) {
    var nameString;

    //console.log(people.findOne());

    /*    function getNameByRid() {
            people.findOne({
                'rid': rid
            }).name;
        }
    */

});
